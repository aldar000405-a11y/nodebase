# You are designing the authentication...

This is a code bundle for You are designing the authentication.... It's based on default Vite project. The original design is available at https://app.flowstep.ai/file?activeFileId=d598feb6-a529-4855-8abc-9605413414f4.

## Running the code

Run `npm i` and `npm run setup` to install the dependencies.

Run `npm run dev` to start the development server.
