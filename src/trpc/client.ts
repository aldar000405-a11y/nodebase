// Re-export all from client.tsx - this file exists because TypeScript prefers .ts over .tsx
// The actual implementation is in client.tsx
export { TRPCReactProvider, useTRPC, trpc } from "./client.tsx";
// ok it worked and now i have another design nodes issue as you see first image please focuse too much on borders of two http request nodes i want exactly to mine also like that exactly 